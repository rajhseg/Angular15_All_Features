import { RforDirective } from './rfor.directive';

describe('RforDirective', () => {
  it('should create an instance', () => {
    const directive = new RforDirective();
    expect(directive).toBeTruthy();
  });
});
